# 검증 기록

2026-09-22 기준.

## 실제 서비스 E2E

회사 저장소에 접근하지 않았습니다. `pwdff-console-e2e`의 실제 테스트는 **0건 실행**했습니다. 전반부 세 시나리오는 담당자 구술을 구조화한 것이고, 후반부 세 시나리오는 아직 도입 전입니다. 통과율·케이스 수·실행 시각을 이 자료에 채우지 않았습니다.

## 발표 사이트

수행한 것:

- JavaScript 구문 검사 통과: `dist/app.js`, `dist/scenarios.js`.
- `node --test tests/presentation.test.mjs`: 8개 검사 통과.
- 25개 단계가 각각 고유한 재현 화면에 연결되고, 그 화면이 빈 마크업이 아님을 확인했습니다.
- 현황 플레이어와 AI 플레이어가 서로의 단계를 움직이지 않음을 확인했습니다.
- 이전·초기화·시나리오 변경 후 화면과 텍스트가 함께 복원되는지 확인했습니다.
- 로그인 실패 화면이 성공 화면으로 바뀌지 않는지, pub 화면이 자동 알림 경로로 표시되지 않는지 확인했습니다.
- 상태 문자열이 전반부 `결과 미연결`, 후반부 `도입 예정`으로 유지되고 검증 범위 목록에 그대로 나타나는지 확인했습니다.
- 배경 카드 순환, 대본·설계 모달, 모달이 발표 단축키를 가로막는 동작, 발표 모드 토글과 6개 장면 키보드 이동을 검사했습니다.
- HTML id와 app.js 참조, 섹션 앵커, 선언된 screen과 구현된 screen, JavaScript가 쓰는 CSS 클래스의 정의 여부를 교차 대조해 불일치 0건을 확인했습니다.
- 로컬 HTTP 서버의 진입점 200 응답을 확인했습니다.

## 움직임 레이어 (2026-09-22 추가분)

이번에는 **실제 브라우저에서 확인했습니다.** Chrome 153 headless를 DevTools 프로토콜로 띄워 1600×1000으로 여섯 섹션과 단계 전환을 캡처해 눈으로 봤고, 아래를 계측했습니다.

- 가로 스크롤 넘침: 390 / 1440 / 1920 px에서 각 섹션마다 4초씩 `scrollWidth`를 관측해 뷰포트 폭과 동일함을 확인했습니다(넘침 0).
- 계측 중 결함 1건을 찾아 고쳤습니다. 검증 배경 섹션의 성운 빛을 확대(`scale`)하자 그 레이어가 문서를 넓히고, 넓어진 문서가 다시 레이어를 넓히는 되먹임이 생겨 가로 폭이 초당 약 13px씩 무한히 늘었습니다. 확대를 빼고 밝기만 변하도록 바꿨습니다.
- `prefers-reduced-motion: reduce`를 켠 상태로 열어 `body`에 `motion` 클래스가 붙지 않고, 검증 범위·제목·발표 자료 카드 중 불투명도가 낮은 요소가 0개임을 확인했습니다. 즉 움직임을 끈 환경에서 내용이 숨겨지지 않습니다.
- 여섯 섹션 이동, 양쪽 플레이어의 모든 단계 진행, 발표 모드 진입, 대본 모달 열기를 한 세션에서 실행하는 동안 콘솔 오류·경고가 0건이었습니다.
- 발표 모드(헤더 62px)와 390×844 모바일에서 진행 막대 위치와 레이아웃을 캡처로 확인했습니다.

여전히 하지 않은 것: 실제 노트북의 물리 디스플레이 확인, 프로젝터 출력, 다른 브라우저(Safari·Firefox), 저사양 기기에서의 프레임 저하 측정.

## 수행하지 않은 것

`node --test`의 8개 검사는 DOM 모형 기반이며 **실제 브라우저 E2E가 아닙니다.** 여기서 통과한다고 화면이 제대로 보인다는 뜻은 아닙니다.

브라우저 확인은 움직임 레이어를 넣으면서 처음 했고, 범위는 위에 적은 만큼입니다. headless Chrome 한 종류이며, 사람이 마우스로 하는 클릭 QA, 전체화면과 키보드 포커스의 실환경 확인, 프로젝터 출력, Safari·Firefox, 저사양 기기 프레임 측정은 하지 않았습니다.

CSS는 760px / 1100px / 1700px 기준으로 구성했고 reduced-motion을 지원합니다. 발표 전에 실제 노트북에서 한 번 열어 확인하시기를 권합니다.
