# 검증 기록

2026-09-22 기준.

## 실제 서비스 E2E

회사 저장소에 접근하지 않았습니다. `pwdff-console-e2e`의 실제 테스트는 **0건 실행**했습니다. 전반부 세 시나리오는 담당자 구술을 구조화한 것이고, 후반부 세 시나리오는 아직 도입 전입니다. 통과율·케이스 수·실행 시각을 이 자료에 채우지 않았습니다.

## 발표 사이트

수행한 것:

- JavaScript 구문 검사 통과: `dist/app.js`, `dist/scenarios.js`.
- `node --test tests/presentation.test.mjs`: 7개 검사 통과.
- 25개 단계가 각각 고유한 재현 화면에 연결되고, 그 화면이 빈 마크업이 아님을 확인했습니다.
- 현황 플레이어와 AI 플레이어가 서로의 단계를 움직이지 않음을 확인했습니다.
- 이전·초기화·시나리오 변경 후 화면과 텍스트가 함께 복원되는지 확인했습니다.
- 로그인 실패 화면이 성공 화면으로 바뀌지 않는지, pub 화면이 자동 알림 경로로 표시되지 않는지 확인했습니다.
- 상태 문자열이 전반부 `결과 미연결`, 후반부 `도입 예정`으로 유지되고 검증 범위 목록에 그대로 나타나는지 확인했습니다.
- 배경 카드 순환, 대본·설계 모달, 모달이 발표 단축키를 가로막는 동작, 발표 모드 토글과 6개 장면 키보드 이동을 검사했습니다.
- HTML id와 app.js 참조, 섹션 앵커, 선언된 screen과 구현된 screen, JavaScript가 쓰는 CSS 클래스의 정의 여부를 교차 대조해 불일치 0건을 확인했습니다.
- 로컬 HTTP 서버의 진입점 200 응답을 확인했습니다.

## 수행하지 않은 것

위 검사는 DOM 모형 기반이며 **실제 브라우저 E2E가 아닙니다.** 브라우저를 통한 클릭 QA, 스크린샷 비교, 1920×1080·1440×900 발표 해상도의 실물 확인, 모바일·전체화면·키보드 포커스의 실환경 검증은 하지 않았습니다. 새로 추가한 25개 재현 화면의 시각적 완성도는 코드로만 확인했고 눈으로 보지 않았습니다.

CSS는 760px / 1100px / 1700px 기준으로 구성했고 reduced-motion을 지원합니다. 발표 전에 실제 노트북에서 한 번 열어 확인하시기를 권합니다.
