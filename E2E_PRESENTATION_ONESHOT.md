# pwdff-console-e2e 발표 사이트 · 원샷 프롬프트

아래 `---` 이후 전체를 **`pwdff-console-e2e` 저장소를 열어 둔 코딩 AI**에게 그대로 전달하세요.

이 발표 구성본(`dist/`)을 함께 가져갔다면 저장소 루트에 두고 첫 줄에 `기존 구성: ./presentation` 처럼 경로를 덧붙이세요. 그러면 새로 만들지 않고 그 구성을 실제 근거로 채웁니다. 안 가져갔으면 프롬프트만으로 새로 만듭니다.

디자인 레퍼런스: https://astraeoratio.plaync.com/ko-kr/index

---

너는 프런트엔드 개발자이자 QA 엔지니어이며, 복잡한 테스트를 비개발자도 이해할 수 있는 이야기로 만드는 발표 설계자다. 이 저장소의 실제 코드를 읽고 **실행 가능한 한국어 발표용 웹사이트**를 완성해라. 계획이나 설명만 제출하지 말고 분석, 구현, 실행, 검증까지 수행해라.

## 0. 이미 확정된 사실 — 다시 묻지 마라

담당자 구술로 확인된 내용이다. 이것과 저장소 코드가 어긋나면 **코드를 우선하고, 어긋난 지점을 보고해라.**

**지금 돌고 있는 것 (전반부 주제)**

- 실행 환경은 NCP 독립망이다. 외부에서 접근할 수 없다.
- Playwright headless로 콘솔을 테스트한다.
- 매일 오전 **08:50**에 풀 테스트를 1회 실행한다.
- **1시간마다** 로그인 테스트를 실행한다.
- 로그인 테스트는 **실패할 때만** 메일을 발송한다. 성공하면 알림이 없다.
- **dev 콘솔만** 로그인 자동화가 가능해 자동 실행 대상이다.
- **pub 콘솔**은 담당자가 직접 로그인한 뒤 수동으로 실행한다.

**앞으로 할 것 (후반부 주제 · AI-assisted E2E Testing)**

대상 환경은 독립망이 아니라 **업무망**이다. 아직 도입 전이며 실행 결과가 없다.

1. **2차 인증 통과** — Google Mail MCP를 연결해 AI가 테스트 계정 메일함에서 인증 코드를 읽어 2단계 인증을 넘긴다. 지금까지 자동화를 막던 벽이고, pub 콘솔이 수동으로 남은 이유와 이어진다.
2. **다국어 레이아웃 검사** — 언어를 영어로 바꿨을 때 글자 길이가 달라져 레이아웃이 깨지는 페이지를 AI가 화면을 직접 보고 찾는다.
3. **기획서 기반 QA** — 기획서를 AI에게 학습시켜 실제 QA가 하듯 확인 항목을 도출하고 실행한다.

이 셋은 전반부에서 드러나는 한계 셋과 1:1로 대응한다. 발표에서 그 연결이 보이게 구성해라.

- 2차 인증이 있으면 로그인 자체가 막힘 → 1번
- 눈으로 봐야 아는 문제는 assertion으로 쓰기 어려움 → 2번
- 확인할 항목을 사람이 먼저 정해줘야 함 → 3번

## 1. 저장소에서 찾아 채워라

전반부는 추정하지 말고 코드에서 확인해라.

- 테스트 파일과 테스트 이름, 실제 assertion, 확인한 줄 번호
- 실행 명령과 스케줄 설정(cron·CI·스케줄러 등 실제로 쓰는 것)
- 08:50과 1시간 주기가 어디에 정의돼 있는지
- 메일 발송 구현, 수신자 설정 위치, 실패 판정 기준, 재시도 여부
- 리포트·스크린샷·트레이스 산출 경로와 보관 방식
- dev와 pub의 설정 차이, pub 수동 실행 절차
- 전체 테스트 목록과, 그중 발표에 쓸 핵심 흐름

후반부는 아직 코드가 없을 가능성이 높다. 있으면 연결하고, 없으면 **'도입 예정 · 미실행'으로 표시하고 지어내지 마라.** 대신 도입 시 정해야 할 항목을 사이트에 남겨라: 전용 테스트 계정과 권한 범위, 대상 페이지 범위와 허용 오차, 기획서 반입 경로와 버전 관리, 도출 항목의 승인 절차.

## 2. 정직성 규칙 — 가장 중요하다

- 코드를 읽었다는 사실을 테스트가 통과했다는 뜻으로 쓰지 마라.
- 기존 보고서를 현재 실행 결과처럼 취급하지 마라. 실행 시각과 환경을 함께 표시해라.
- 실행 근거가 없으면 통과율·수치 대신 **미실행**과 필요한 환경을 보여라.
- 제안 시나리오는 기존 테스트와 구분하고 통과율 집계에서 제외해라.
- 재시도로 통과한 테스트는 그 사실을 표시해라.
- mock으로 검증한 범위와 실제 서버를 거친 범위를 구분해라.
- 단계 재생은 시뮬레이션이다. '다음 단계'를 눌렀다는 이유로 상태를 통과로 바꾸지 마라.
- 계정, 토큰, 내부 호스트명, 실제 URL, 개인정보를 화면·캡처·로그·커밋에 남기지 마라.
- 저장소가 비어 있거나 접근이 막히면 지어내지 말고 필요한 경로를 한 번에 요청해라.

## 3. 만들 것

디자인 콘셉트는 **반짝이는 은하**다. 짙은 남색 우주, 보랏빛·청록빛 성운, 작고 섬세한 별빛, 큰 흰색 세리프 제목. 레퍼런스 사이트의 화면 구성, 크기 비례, 여백, 대비, 카드 형태, 섹션 배치를 살리되 콘텐츠는 이 프로젝트의 실제 내용으로 바꾼다. 검은 배경에 카드 몇 개를 붙이는 수준으로 축소하지 마라. 시연과 본문은 밝고 선명한 면 위에 올려 읽기 쉽게 한다. 별빛은 발표 모드와 reduced-motion에서 멈춘다.

청중은 비개발자가 포함된 팀, 시간은 약 10분이다. 한 화면에 핵심 메시지 하나와 그것을 이해시키는 장면 하나. 제목과 설명은 이 프로젝트의 실제 업무 용어로 쓴다.

**여섯 장면으로 구성해라.**

1. **소개** — 큰 비주얼과 제목. 부제에 `AI-assisted E2E Testing`이라는 용어를 명시한다. 지금의 현황과 앞으로의 방향을 한 문장씩 예고한다.
2. **검증 배경** — 중앙 카드가 강조되는 슬라이더. 어디서(독립망→업무망), 언제(08:50·매시간), 어디까지(지금 못 하는 것 세 가지)를 담는다. 세 번째 카드가 후반부의 예고가 되게 한다.
3. **현재 E2E** — 발표의 앞 중심. 왼쪽에 콘솔 재현 화면, 오른쪽에 시나리오·현재 단계·기대 결과. 하단 선택기로 풀 테스트 / 로그인 감시 / pub 수동 실행을 전환한다. 이전·다음·처음부터 제어와 단계 표시를 둔다.
4. **AI-assisted E2E** — 발표의 뒤 중심. 3번과 같은 구조를 쓰되 시각적으로 구분되게 한다(예: 강조색 변경). 2차 인증 통과 / 다국어 레이아웃 / 기획서 기반 QA 세 가지를 같은 방식으로 시연한다. 상태는 전부 **도입 예정**이다.
5. **검증 범위** — 전반부와 후반부를 그룹으로 나눠 실행 상태와 그것이 의미하는 바를 보여준다. 실측 데이터가 있으면 그 데이터로 집계하고, 없으면 미실행으로 둔다. 이 자료에 아직 없는 범위를 명시한다.
6. **발표 자료** — 약 10분 대본, 이 원샷 프롬프트, 검증 설계를 연다. 실제 캡처·트레이스가 있으면 열람 가능한 갤러리를 만들고, 없는 자료를 가짜 썸네일로 채우지 마라.

각 단계에서 다음 세 가지가 한눈에 연결되게 배치해라.

1. 무엇이 일어나는가
2. 화면·상태에 나타나는 변화
3. 테스트가 확인하는 사실

테스트가 특정 문구 노출만 확인한다면 그 사실만 설명하고 서버 저장까지 검증했다고 확대하지 마라. 코드와 assertion은 '검증 근거 보기'를 눌렀을 때 펼쳐지게 하고, 기본 화면은 비개발자가 이해할 수 있는 말로 쓴다.

## 4. 발표 중 사용할 기능

- 상단 메뉴 여섯 개와 현재 위치 표시.
- 발표 모드에서 개발 정보를 감추고 본문과 시연을 크게 보여준다.
- 방향키로 시나리오 단계를 이동한다. **3번과 4번 두 섹션 모두**에서 동작해야 하며 서로의 단계를 움직이면 안 된다. PageUp/PageDown은 장면 이동, Home/End는 처음·마지막, Esc는 종료.
- 입력 요소에 포커스가 있거나 모달이 열려 있으면 전역 단축키가 가로채지 않는다.
- 전체화면 버튼을 제공하고, 허용되지 않으면 일반 화면에서 그대로 쓸 수 있게 한다.
- 발표 대본은 본문을 가리지 않는 별도 보기로 제공한다. 약 10분의 장면별 멘트를 쓴다.
- 1920×1080과 1440×900에서 잘 보이게 하고, 작은 화면에서는 순서대로 읽히게 재배치한다. 발표 모드 본문은 24px 이상을 목표로 하되 넘침을 막는다. 키보드 포커스, 텍스트 대비, 버튼 이름을 갖춘다.
- 자동 전환과 자동 오디오 재생은 하지 않는다.

## 5. 구현과 데이터

기존 저장소의 프레임워크와 패키지 관리 방식을 우선한다. 발표 사이트를 테스트 코드와 분리해 추가하고, 운영 설정·테스트 의미를 발표 목적으로 바꾸지 마라. 적절한 위치가 없으면 `presentation/`에 독립된 앱을 만들고 원본은 보존한다.

시나리오 데이터와 화면 컴포넌트를 분리한다. 전반부와 후반부를 별도 배열로 두고, 같은 단계 플레이어가 둘 다 렌더링하게 만든다. 근거 파일 위치와 표시 상태가 데이터에서 연결되게 하고, 보고서가 있을 때만 결과를 가져온다. 데이터가 없으면 빈 상태가 정직하게 드러나야 한다.

핵심 발표는 서버를 켠 뒤 외부 API나 원격 이미지가 끊겨도 진행 가능해야 한다. 실제 테스트 재실행을 발표 장면 이동에 연결하지 마라. 기본 산출물은 로컬 실행본이며, 요청받지 않았다면 외부에 게시하거나 업로드하지 마라.

## 6. 끝까지 검증해라

원본 테스트 검증과 발표 사이트 검증을 **별개로** 수행하고 보고해라.

원본 테스트는 기존 문서의 실행 방법을 확인해 준비된 환경에서 실행한다. 환경이나 계정이 없으면 정확한 실행 장애를 기록한다. 운영 데이터를 바꾸는 흐름을 임의로 실행하지 마라. 실패를 숨기려고 assertion을 완화하거나 성공으로 하드코딩하지 마라.

발표 사이트는 빌드와 실행을 확인하고 브라우저에서 직접 점검해라.

- 메뉴 이동과 현재 위치, 발표 시작·종료
- 카드·시나리오 선택, 두 섹션 모든 단계의 이전·다음·초기화
- 두 섹션의 단계가 서로 간섭하지 않는지
- 근거 열기·닫기, 키보드 이동과 전체화면
- 시뮬레이션 재생이 실제 테스트 상태를 바꾸지 않는지
- 실제 데이터와 결과 집계의 일치, 미실행·도입 예정 표시
- 발표 해상도와 모바일에서 겹침·잘림·의도치 않은 가로 스크롤이 없는지
- 새로고침 후 정상 진입, 이미지 누락과 치명적 콘솔 오류가 없는지

가능하면 이 저장소의 Playwright로 핵심 조작을 자동 검증해라. 환경 제약으로 못 한 확인은 **완료했다고 보고하지 마라.**

## 7. 산출물

1. 실행 가능한 발표 사이트와 시나리오 데이터(전반부·후반부 분리)
2. 코드·테스트 근거와 선정 이유를 담은 `E2E_ANALYSIS.md` — 확정된 사실과 코드가 어긋난 지점 포함
3. 레퍼런스 관찰과 구현 대응을 담은 `DESIGN_REFERENCE.md`
4. 약 10분 장면별 대본 `PRESENTATION_SCRIPT.md`
5. 실행·조작·데이터 갱신 방법을 담은 README
6. 수행한 검증과 미수행 범위를 구분한 `VALIDATION.md`

완료 응답에는 사이트 여는 방법, 연결한 실제 근거, 원본 E2E의 실행 상태, 발표 사이트 검증 결과, 남은 제한을 간결하게 적어라. 정보가 충분하면 중간 승인 없이 완성까지 진행해라. 분석 대상이 없거나 필수 접근 정보가 없을 때만 구체적으로 요청해라.
